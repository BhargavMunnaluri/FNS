int  g = 17, n = 13, x = 6, y = 4;

	int A = (int)(pow(g, x)) % n; // public key of A
	int B = (int)(pow(g, y)) % n; // public key of B

	int keyA = (int)(pow(B, x)) % n; // secret key of A
	int keyB = (int)(pow(A, y)) % n; // secret key of B
	cout << " Secret key of A & B  is : " << keyA << endl;
