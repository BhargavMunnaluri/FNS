int p = 3, q = 11, n = p * q, phi = (p - 1) * (q - 1), e = 7, d = 3, msg;
	
	cout<< "Enter message less than 33 ";
	cin >> msg;
	int c = 1, m = 1;
	for (int i = 0; i < e; i++) c = (c * msg) % n;
	for (int i = 0; i < d; i++) m = (m * c) % n;
	cout << "Encrypted:" << c << " Decrypted:" << m;
